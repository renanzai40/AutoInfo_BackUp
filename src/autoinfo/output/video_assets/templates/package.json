{
  "name": "autoinfo-video",
  "version": "1.0.0",
  "description": "AutoInfo HyperFrames video project — generated by autoinfo.output.video",
  "scripts": {
    "lint": "bun x hyperframes lint",
    "render": "bun x hyperframes render --output out/video.mp4",
    "render:draft": "bun x hyperframes render --output out/video.mp4 --quality draft",
    "render:standard": "bun x hyperframes render --output out/video.mp4 --quality standard"
  },
  "dependencies": {
    "hyperframes": "^0.6.95"
  }
}
