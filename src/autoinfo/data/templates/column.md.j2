# {{ title }}

**Domain**: {{ domain }}  
**Generated**: {{ generated_at }}  
{% if collection_id %}**Collection**: {{ collection_id }}  
{% endif %}**Sections**: {{ sections|length }}  
**References**: {{ references|length }}  
**Cadence**: Weekly column

---

## The Big Idea

{{ executive_summary }}

---

## Deep Dive

{% for section in sections %}
### {{ section.title }}

{{ section.content }}

{% if section.entries %}
| # | Title | Summary |
|---|-------|---------|
{% for entry in section.entries %}
| {{ loop.index }} | {{ entry.title }} | {{ entry.summary | product_summary or "—" }} |
{% endfor %}
{% endif %}

{% else %}
_No deep-dive sections available for this column._
{% endfor %}

---

## What Changed This Week

Top developments this week (full source list in References):

{% set ns = namespace(shown=0) %}
{% set seen_titles = sections | selectattr("entries") | map(attribute="entries") | map("map", attribute="title") | map("join", "|") | join("|") | lower %}
{% set selected = [] %}
{% for ref in references %}
{% if ns.shown < 8 and (ref.title | lower) not in seen_titles %}
{% set _ = selected.append(ref) %}
{% set ns.shown = ns.shown + 1 %}
{% endif %}
{% else %}
_No developments this week._
{% endfor %}
{% if not selected and references %}
{% set selected = references[:8] %}
{% endif %}
{% for ref in selected %}
- **{{ ref.title }}**{{ (" — " ~ ref.source_url) if ref.source_url else "" }}{{ (" (" ~ ref.source_label | default(ref.source_platform, "") | platform_name ~ ")") if (ref.source_platform or ref.source_label) else "" }}
{% endfor %}

---

## Implications & Outlook

{% for sec in sections %}
{% set _impl = implications[loop.index0] if (implications and implications[loop.index0] is defined and implications[loop.index0]) else ("Covered in the Deep Dive (" ~ (sec.entries|length) ~ " item(s)); watch for follow-up developments next period.") %}
- **{{ sec.title }}** — {{ _impl }}
{% else %}
_No outlook sections available._
{% endfor %}

## Reader Takeaways

{% if action_required %}
{% for a in action_required %}
{{ loop.index }}. {{ a }}
{% endfor %}
{% elif recommendations %}
{% for r in recommendations %}
{{ loop.index }}. {{ r }}
{% endfor %}
{% else %}
{% for sec in sections %}
{{ loop.index }}. Review the {{ sec.title }} section for the underlying developments.
{% endfor %}
{% endif %}

{% if appendices %}
---

## Appendices

{% for appendix in appendices %}
### {{ appendix.title }}

{{ appendix.content }}

{% endfor %}
{% endif %}

---

*AutoInfo Column · {{ domain }} · {{ generated_at }}*
