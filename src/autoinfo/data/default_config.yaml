# AutoInfo default configuration template.
# Generated by `autoinfo init --demo <domain>`.
# See https://github.com/your-org/autoinfo#readme for documentation.

project:
  name: "My AutoInfo"
  # project_name: ""           # optional human-friendly project name
  created_at: "2026-07-20"

llm:
  provider: openrouter
  model: deepseek/deepseek-chat
  api_key: ${AUTOINFO_LLM_API_KEY}
  # base_url: "https://api.openai.com/v1"  # optional: custom API endpoint
  json_mode: false
  # reasoning_model: false  # set to true for reasoning models that don't support response_format
  # --- Model pool (uncomment to configure; see configure_llm(llm_fallback=..., llm_tasks=...)) ---
  # fallback:
  #   - model: mimo-v2.5
  #     base_url: "https://opencode.ai/zen/go/v1"
  # tasks:
  #   extraction:
  #     model: deepseek-v4-flash
  #     provider: openai
  #     max_tokens: 4096

domains: []

# --- v1.2 optional sections (uncomment to configure) ---

# cefr:
#   enabled: false
#   languages: ["en", "zh", "ja"]
#   model: ""

# email:
#   enabled: false
#   smtp_host: ""
#   smtp_port: 587
#   smtp_user: ""
#   smtp_pass: ${AUTOINFO_SMTP_PASS}
#   from_addr: ""
#   to_addrs: []

# rest_api:
#   enabled: true
#   port: 8741
#   host: "127.0.0.1"

# vector_search:
#   enabled: false
#   model: ""
#   hybrid_weight_fts5: 0.7
#   hybrid_weight_vector: 0.3

# cron:
#   auto_install: false
#   install_path: ""

# multi_user:
#   enabled: false
#   default_user_id: "default"

# --- v1.9 TTS configuration (uncomment to configure) ---

# tts:
#   engine: openai  # "openai" (default) or "local" (edge-tts, free, offline)
#   # local_voice: "en-US-JennyNeural"  # voice for edge-tts
